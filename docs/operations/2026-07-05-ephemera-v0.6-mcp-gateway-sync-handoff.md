# Ephemera v0.6 MCP Gateway Sync Handoff (Phase 3)

- 작성일: 2026-07-05
- 대상 branch: `sync/ephemera-v0.7-core-service-parity` (HEAD `04e2a12`)
- 상태: Phase 3 완료. upstream ephemera `v0.6.0`-`v0.6.4`(runtime MCP Gateway)를 anvil
  main runtime baseline으로 병합·적응하고 full KVM gate로 검증했다(e2e `334✓/0✗`).
  `v0.7.0`은 미병합 backlog로 남는다.
- 선행: [`docs/operations/2026-07-05-ephemera-v0.5-operator-sync-handoff.md`](2026-07-05-ephemera-v0.5-operator-sync-handoff.md) (Phase 2)

## 가장 중요한 문서 요구사항 — 두 "MCP"의 분리

이 sync가 도입한 **runtime MCP Gateway**(`EPHEMERA_MCP_*`, `internal/mcpgateway`)는
기존 **IronClaw MCP adapter**(`ANVIL_MCP_*`, `cmd/anvil-mcp`)와 이름만 비슷할 뿐 다른
계층이다.

| 구분 | IronClaw MCP adapter | runtime MCP Gateway |
|---|---|---|
| 목적 | IronClaw가 anvil VM lifecycle을 `anvil_*` tool로 호출 | VM 내부 agent가 host가 중개하는 backend MCP server를 사용 |
| 방향 | north-bound (IronClaw → anvil) | south-bound (guest agent → daemon → backend) |
| 구현 | `cmd/anvil-mcp`, `internal/anvilmcp` | `internal/mcpgateway`, `cmd/goose-daemon` |
| 설정 | `ANVIL_MCP_*` | `EPHEMERA_MCP_*` |
| surface | IronClaw MCP surface | runtime/operator surface |

**runtime MCP Gateway는 `cmd/anvil-mcp` IronClaw adapter를 대체하지 않는다.** gateway
tool은 IronClaw `anvil_*` tool 목록·schema에 추가되지 않는다(guard). KVM gate에서
runtime gateway가 live인 상태로 `anvil-mcp-e2e.sh` lifecycle·flock이 PASS해 adapter가
영향받지 않음을 확인했다.

## Phase 3에서 반영한 내용

upstream `v0.6.0`-`v0.6.4`(upstream에 `v0.6.3` 없음)가 anvil main runtime baseline으로
채택/적응됐다. 분류는 [`docs/ADR_INDEX.md`](../ADR_INDEX.md) Section 4,
[`docs/PUBLIC_RELEASE_BOUNDARY.md`](../PUBLIC_RELEASE_BOUNDARY.md) Section 5,
[`docs/operations/upstream-sync-policy.md`](upstream-sync-policy.md)를 기준으로 한다.

| tag | 상태 | 요지 |
|---|---|---|
| `v0.6.0` | adapted | runtime MCP Gateway(`internal/mcpgateway`, daemon `/config/mcp*` handler, `configs/mcp/*.example`, Web UI MCP console). runtime/operator surface(IronClaw MCP 아님) |
| `v0.6.1` | adapted | `EPHEMERA_NET_ANTISPOOF` 기본 on(ebtables best-effort), per-(VM,server) token-bucket rate limit(`EPHEMERA_MCP_RATE`/`BURST`) |
| `v0.6.2` | adapted | resources/prompts aggregate + per-tool/per-profile policy, resources/prompts가 tools와 policy·rate bucket 공유, audit `kind` field |
| `v0.6.4` | adapted | stdio backend(subprocess), child env 재구성, `credential_env`, root면 `nobody`+scratch, process-group reap, `GET /config/mcp/servers` `has_credential`만 |

대표 병합/적응 commit: `v0.6.0` merge `6e42d2b`/adapt `cf2e87a`, `v0.6.1` merge
`9ba10f0`/adapt `74c89c4`, `v0.6.2` merge `94fafdf`/adapt `688e6ad`, `v0.6.4` merge
`1bdd491`/adapt `04e2a12`.

## anvil 경계 — 구조적으로 강제

- **caller identity**: caller profile은 request source IP를 VM registry(`10.0.1.x`)와
  대조해 server-side로 판정한다. registry에 없는 caller는 `403`이며 guest가 identity를
  주입하지 못한다.
- **credential**: backend credential은 host-side(`configs/mcp/secrets.yaml`, gitignored)
  에만 있다. `VMPrepareOptions`에 credential 필드가 없어 VM에는 gateway URL만 주입된다.
  stdio backend credential은 `credential_env`로만 child에 주입하고 argv에는 넣지 않는다.
- **audit**: `audit/mcp.jsonl`은 고정 key set metadata만 기록하고 tool argument/result와
  `Err` 문자열까지 제외한다(metadata-only, sentinel test).
- **policy**: profile policy는 `servers.yaml`을 좁히기만 하고(intersection) 넓힐 수 없다.
- **anvil boundary guard 4종**(v0.6.0): IronClaw schema/adapter가 gateway tool 제외,
  audit metadata-only sentinel, `/config/mcp*` bearer 없으면 `401`, VM은 URL(credential
  아님)만 받고 policy는 widen 불가.

## Gate 결과

- CI-safe gate all green: `git diff --check`, targeted test group(mcpgateway/boundary/
  audit/ratelimit/stdio guard 포함), web build 재현 가능(`uidist` drift 없음),
  `go test ./... -count=1`(EXIT=0), `go build ./cmd/{goose-daemon,anvil-mcp,anvil-scheduler}`.
- 실제 KVM host:
  - `sudo bash e2e_test.sh` `334✓ / 0✗`("All test steps passed"). gateway step `84`-`89`
    최초 실행 green. provider-key skip 3건(LLM smoke, real tool call, real stdio tool call).
  - `scripts/anvil-mcp-e2e.sh lifecycle` PASS, `scripts/anvil-mcp-e2e.sh flock` PASS
    (runtime gateway live 상태에서 adapter 영향 없음).
  - `sudo -n bash scripts/vm-workload-e2e.sh` PASS.
  - `scripts/anvil-mcp-e2e.sh semantic`은 key-free 구간(workspace copy-in/out, task
    proxy, cleanup)이 `200`이고 LLM-echo substep만 known-invalid local Google key로
    실패했다. provider-key 의존 실패이며 코드 결함이 아니다(Phase 2와 동일 기록).

## 운영 주의

- stdio backend를 켤 때: child는 root 실행 시 `EPHEMERA_MCP_STDIO_USER`(기본 `nobody`)로
  drop되고 `/var/lib/ephemera/mcp-stdio` 아래 per-server scratch를 cwd·HOME으로 쓴다.
  scratch base와 상위 디렉터리 권한을 확인한다.
- `EPHEMERA_MCP_BIND_IP`는 기본 안전한 bridge IP bind를 override할 수 있다. default가
  안전하며, source-IP `403`이 defense-in-depth로 남는다.
- KVM gate 스크립트는 gitignored 로컬 operator 파일 `configs/goose.yaml`,
  `configs/goose-secrets.yaml`이 있어야 한다. gateway backend를 실제로 붙이려면
  `configs/mcp/servers.yaml`·`configs/mcp/secrets.yaml`도 필요하다(gitignored, release
  artifact에 포함하지 않는다).

## Deferred / 미채택

- `v0.4.4` flock broadcast의 MCP tool 노출, `v0.4.2` default COW 전환, `v0.4.0`
  auto-snapshot public support(이전 phase에서 이미 deferred).
- `v0.7.0`(installer/transcript/hardening)은 미병합 backlog. kernel SHA / `waitForAgent`
  per-probe timeout / `EPHEMERA_HOME`은 baseline sync와 독립 backport로 이미 반영됐지만
  tag 전체 채택은 아니다.

## Next Action

- `sync/ephemera-v0.7-core-service-parity`의 다음 목표인 `v0.7.0` adoption review
  (installer/transcript/hardening)와 anvil 공개 경계 분류를 별도 sync/analysis로
  진행한다.

## Follow-Up Tasks

- runtime MCP Gateway backend 운영 정책(profile↔backend 바인딩, rate-limit·credential
  운영)과 실제 operator 배포 검증.
- flock member spawn이 per-profile `EPHEMERA_VCPU_COUNT`/`EPHEMERA_MEM_SIZE_MIB`
  override를 존중하도록 sizing 경로 정리(v0.5 follow-up 유지).
- proxy agent client keep-alive 비활성화(`64ec57c`)의 upstream 기여 검토(v0.5 follow-up
  유지).
- `v0.4.2` default COW 전환과 `v0.4.4` flock broadcast MCP tool 노출 결정.
- upstream ephemera `v0.7.0` adoption review와 anvil 공개 경계 분류.
- 향후 `anvil-v*` release 후보가 이 baseline을 포함하면
  [`docs/operations/release-checklist.md`](release-checklist.md)의 KVM gate 필수
  스크립트(+gateway step 84-89)와 env 전제(configs/goose*.yaml, configs/mcp/*)를 함께
  확인한다.
