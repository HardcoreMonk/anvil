# Ephemera v0.7 Parity Sync Handoff (Phase 4 — parity scope complete)

- 작성일: 2026-07-06
- 대상 branch: `sync/ephemera-v0.7-core-service-parity` (HEAD `7b3f009`)
- 상태: Phase 4 완료. upstream ephemera `v0.7.0`(end-user installer + conversation
  transcript restore + upstream hardening reconcile)을 anvil main runtime baseline으로
  병합·적응하고 full KVM/installer gate로 검증했다(e2e `334✓/0✗`). **이로써 upstream
  parity scope(`v0.4.0`-`v0.7.0`)의 코드 편입이 완료됐다.**
- 선행: [`docs/operations/2026-07-05-ephemera-v0.6-mcp-gateway-sync-handoff.md`](2026-07-05-ephemera-v0.6-mcp-gateway-sync-handoff.md) (Phase 3)

## Phase 4에서 반영한 내용

upstream `v0.7.0`가 anvil main runtime baseline으로 채택/적응됐다. 분류는
[`docs/ADR_INDEX.md`](../ADR_INDEX.md) Section 4,
[`docs/PUBLIC_RELEASE_BOUNDARY.md`](../PUBLIC_RELEASE_BOUNDARY.md) Section 2/5,
[`docs/operations/upstream-sync-policy.md`](upstream-sync-policy.md)를 기준으로 한다.
대표 commit: merge `b2df010`, adapt `7b3f009`.

### end-user installer — runtime/operator surface

`install.sh` / `uninstall.sh` / `INSTALL.md` / `ephemera.service.in`와 release
workflow(`scripts/build_release.sh`)는 **ephemera daemon을 위한 runtime/operator
installer**이며 anvil/IronClaw product wrapper가 아니다. systemd service는 canonical
`ephemera` 이름을 유지한다(rule-permitted, anvil alias wrapper 없음;
`EPHEMERA_HOME=@DEST@`).

- release build integrity: `build_release.sh`가 다운로드한 kernel/firecracker를
  `main.go`에서 parse한 pin과 `sha256sum -c`로 검증한다. runtime `EnsureKernel`이 기존
  파일을 `os.Stat`로 skip하던 FULL-tarball supply-chain gap을 닫는다.
- `uninstall.sh`는 ephemera-scoped `/tmp` scratch를 root-gated·prefix-anchored로
  정리한다(의도된 cleanup). `/tmp/goose-rootfs` 항목은 현재 source에 producer가 없는
  stale no-op이다.
- 외부 Web UI 노출은 reverse proxy/TLS 또는 private network 뒤에서만 한다.

### conversation transcript restore

daemon proxy `GET /vms/{id}/sessions/{name}/transcript`(bearer)가 대화 transcript를
복원한다. agent export는 read-only `goose session export`(model call 없음)이고 응답
schema `{turns:[{role,text}]}`는 auth-free여서 Web UI가 daemon token 없이 렌더한다.

**4 transcript-safety guard(TDD):**

1. endpoint는 bearer 없으면 `401`.
2. payload는 provider key / CP token / `agent_token` sentinel-free.
3. cache-hit는 agent spawn 없이 serve.
4. export argv는 `session export -n {name} --format json`이며 run-token을 거부한다.

### upstream hardening reconcile — backport 승리

사전 독립 backport 3종이 upstream `v0.7.0` 버전을 이기고 single definition으로 남았다
(anvil이 stricter, **net Go diff는 doc-comment-only**):

- kernel SHA atomic verify(temp+rename, 무조건 검증 — upstream보다 strict)
- `resolveWorkDir` / `EPHEMERA_HOME`
- `waitForAgent` per-probe timeout(deadline cap)

기존 anvil adaptation(agent-stamp mount skip, restore-over-`meta.DiskPath`, proxy
`DisableKeepAlives`)은 하나도 rollback되지 않았다.

## Gate 결과

- CI-safe gate all green: `git diff --check`, installer `bash -n` ×3, targeted test
  group(transcript-safety guard 4종 포함), full glob `go test ./... -count=1` EXIT=0,
  web build drift 없음, 3 builds.
- 실제 KVM/installer gate:
  - `sudo bash e2e_test.sh` `334✓ / 0✗`("All test steps passed").
  - `scripts/anvil-mcp-e2e.sh lifecycle` PASS, `flock` PASS. `sudo -n bash
    scripts/vm-workload-e2e.sh` PASS.
  - `scripts/anvil-mcp-e2e.sh semantic`은 standing provider-key caveat(key-free 구간
    `200`, LLM-echo substep만 known-invalid local Google key로 실패 — 코드 결함 아님).
  - `bash install.sh --help` / `bash uninstall.sh --help` OK(시스템 변경 없음).
  - release build를 root로 실행 → SLIM(≈27M) + FULL(≈250M) tarball + `.sha256`
    checksum(dist/ gitignored).

## Known limitations / 운영 주의

- `uninstall.sh`의 `/tmp` scratch 정리는 의도된 cleanup(root-gated, prefix-anchored).
  `/tmp/goose-rootfs`는 stale no-op(현재 source에 producer 없음).
- 외부 Web UI 노출은 reverse proxy/TLS 또는 private network 뒤에서만.
- KVM/installer gate 전제: gitignored 로컬 operator 파일 `configs/goose.yaml`,
  `configs/goose-secrets.yaml`(+gateway backend를 붙이려면 `configs/mcp/*`).

## 남은 항목 (deferred / 비목표 / release-gate)

tag 채택은 완료됐고, 남은 것은 tag가 아니라 다음 항목이다:

- deferred/비목표: `v0.4.4` flock broadcast의 MCP tool 노출, `v0.4.2` default COW 전환,
  `v0.4.0` auto-snapshot public support, flock member spawn의 per-profile sizing 존중,
  runtime MCP Gateway의 IronClaw 표면 승격 금지(비목표 유지).
- release-gate — 2026-07-06 follow-up batch로 코드 항목 4종 **CLOSED**: audit-writer
  JSONL key-set sentinel(`de5a7aa`); stdio stderr → host slog only, VM-facing error에서
  scrub(`4a802f5`, behavior change); `credential_env` reserved-name {`PATH`,`HOME`,`LANG`}
  거부(`0376afa`, behavior change: config validation error); production-mux auth sentinel
  확장(real production mux, `de5a7aa`). 같은 batch가 minor 항목도 닫았다: missing
  default profile config → `404`(`cd2e70b`, 이전 `500`); snapshot GC fail-closed
  (`613a01b`, behavior change: persisted-VM state를 못 읽으면 Errors entry로 plan
  abort·삭제 없음); KVM-free broadcast unit test(`de5a7aa`); e2e/README step-label
  reconciliation(`0625df5`); keep-alive upstream 제안 **DECLINED**(이미 기록됨).
  batch 검증: 13 pkgs ok, review Approved(0 Crit/Imp), full e2e `334✓/0✗` at `ccc5127`,
  PR #17 push. **남은 open gate 항목은 valid provider key로 `semantic` run(e2e step 59,
  사용자 key 교체 대기)뿐이다.**

## Next Action

- release-gate 코드 4종이 닫혔으므로 남은 것은 open gate(valid provider key `semantic`
  run + e2e step 59)와 실제 operator 배포 검증(installer/systemd host, MCP gateway
  backend 운영)이다. `v0.7.0` 이후 새 upstream tag가 관찰되면 별도 sync/adoption
  review로 처리한다.

## Follow-Up Tasks

- open gate: valid provider key로 `semantic` run + e2e step 59(사용자 key 교체 대기).
  나머지 release-gate 코드 4종은 2026-07-06 batch로 닫혔다(위 참조).
- post-merge backlog: reRestore helper extraction, `config_api.go` split, web npm audit,
  MCP `initialize`-error string genericization review(**#36**, new), GC-abort response
  `Protected` list cosmetic(**#37**, new).
- installer 실제 systemd host 배포 검증과 release build(FULL variant) 재현.
- `v0.4.2` default COW 전환·`v0.4.4` flock broadcast MCP tool 노출·flock per-profile
  sizing·auto-snapshot public support 결정(이전 phase follow-up 유지).
- runtime MCP Gateway backend 운영 정책과 operator 배포 검증(Phase 3 follow-up 유지).
- kernel-SHA/`waitForAgent`/`EPHEMERA_HOME` hardening의 upstream 기여 검토(proxy
  keep-alive 비활성화 `64ec57c`의 upstream 제안은 DECLINED).
- 향후 `anvil-v*` release 후보가 이 baseline을 포함하면
  [`docs/operations/release-checklist.md`](release-checklist.md)의 KVM gate 필수
  스크립트(+gateway step 84-89, +installer help, +release build)와 env 전제를 함께
  확인한다.
