# anvil-v0.4.0 Release Handoff — Ephemera core service parity

- 작성일: 2026-07-06
- checkout: main `/data/projects/claude-zone/anvil` (branch `main`)
- release: `anvil-v0.4.0` (successor to `anvil-v0.3.2`) — upstream ephemera core service
  parity `v0.4.0`-`v0.7.0`
- 선행: [`docs/operations/2026-07-06-ephemera-v0.7-parity-sync-handoff.md`](2026-07-06-ephemera-v0.7-parity-sync-handoff.md) (Phase 4 parity handoff)

## 상태

- PR #17 **merged** into `main` at `7423151`("merge: ephemera core service parity through
  v0.7.0 (PR #17)") — 진짜 merge commit(two parents, **no squash**), upstream runtime
  이력 보존.
- 이 release는 `7423151` 위의 `docs: release anvil-v0.4.0` docs 커밋에서 자른다. tag
  `anvil-v0.4.0`은 그 docs 커밋을 가리킨다(SHA는 orchestrator가 태그 시 확정, 아래
  Next Action).
- baseline: anvil main runtime/operator baseline supports upstream ephemera v0.7.0 with
  anvil adaptations for token redaction, tenant/egress, scheduler, audit, and IronClaw
  MCP surface separation. IronClaw `anvil_*` MCP 경계 유지(`cmd/anvil-mcp` 불변);
  runtime MCP Gateway(`EPHEMERA_MCP_*`)·operator Web UI·installer는 runtime/operator
  surface로만 채택돼 adapter를 대체하지 않는다.

## Release gate (main)

- **CI-safe gate all green**: `git diff --check`, `go test ./... -count=1` EXIT=0,
  release/installer/e2e scripts `bash -n` 7종(`install.sh`, `uninstall.sh`,
  `scripts/build_release.sh`, `e2e_test.sh`, `scripts/anvil-mcp-e2e.sh`,
  `scripts/anvil-scheduler-smoke.sh`, `scripts/vm-workload-e2e.sh`), web build 재현
  가능(`cmd/goose-daemon/uidist/` drift 없음).
- **KVM**: MCP `anvil-mcp-e2e.sh` lifecycle+flock PASS, `scripts/vm-workload-e2e.sh`
  PASS, release build PASS(root; SLIM ≈27M + FULL ≈250M tarball + `.sha256`).
- **full e2e (`sudo bash e2e_test.sh`)**: main 병합 커밋(`7423151`) 위 재실행 결과
  **`334✓ / 0✗` 확인 완료**("All test steps passed", 로그 `/tmp/e2e-release-main2.log`,
  provider-key 의존 substep만 skip). parity gate 기준 결과와 동일.
- **artifacts**: GitHub release artifact는 태그 후 clean rebuild + secret scan을 거쳐
  첨부한다(dist/ 는 gitignored). 이 handoff 커밋에는 첨부하지 않는다.

## 회귀 방지로 고친 latent defect (parity gate 중)

3개 pre-existing latent defect(이번 parity 작업이 만든 결함 아님):
`4c1c803`(restore over recorded `meta.DiskPath`), `38fbedc`(golden image mount skip when
agent-stamp current), `64ec57c`(proxy agent client `DisableKeepAlives` — stale pooled
connection reuse across guest-IP recycling).

## parity 범위

- 전 태그 upstream feature 분류: `docs/analysis/11-v0.5.0-v0.7.0-core-service-parity-review.md`
  의 52-feature parity matrix(14 adopted / 30 adapted / 4 deferred / 4 excluded).
- deferred: `v0.4.0` auto-snapshot public support, `v0.4.2` default COW 전환, `v0.4.4`
  flock broadcast의 MCP tool 노출, `v0.5.3` flock member per-profile sizing.
- excluded(의도적 divergence): flock `agent_tokens` response exposure, 46c orphan-delete
  semantics(anvil `409`), upstream keep-alive pooled proxy client(anvil
  `DisableKeepAlives`), runtime MCP Gateway의 IronClaw 표면 승격(비목표).
- release-gate 코드 4종(audit-writer sentinel, stdio stderr scrub, `credential_env`
  reserved names, production-mux auth sentinel)은 2026-07-06 follow-up batch로 닫혔다.

## Next Action

- orchestrator: main 병합 후 `sudo bash e2e_test.sh` 재실행 결과 `334✓ / 0✗` 확인 →
  `docs: release anvil-v0.4.0` 커밋 SHA 확정 → `anvil-v0.4.0` annotated tag 생성·push →
  GitHub Release 게시(본문은 `RELEASE_NOTES.md`의 `# anvil-v0.4.0` 절) → tag-clean
  rebuild + secret scan 후 SLIM/FULL tarball + `.sha256` artifact 첨부. (이 워커는 push
  하지 않는다.)
- orchestrator: zone 인벤토리 등록(`ops/units.yaml`, `ops/projects.yaml`,
  `wiki/entities/`, 필요 시 ADR + L3 CLAUDE.md) — 이번 release에서 orchestrator가 수행.

## Follow-Up Tasks

- **open gate**: valid provider key로 `scripts/anvil-mcp-e2e.sh semantic` 1회 green
  (multi-turn `--resume` live-model e2e), e2e step 59. 사용자 key 교체 대기.
- **Google API key rotation**: 로컬 `configs/goose-secrets.yaml`의 key가 semantic gate에서
  invalid로 거부됨 + 평문 보관 → 회전 권고. gitignore 확인됨(커밋 이력에 없음).
- **post-merge backlog 5건 — 2026-07-06 `cleanup/post-v0.4.0-backlog` 브랜치에서 처리**:
  - 완료 4건: GC-abort `Protected` 보존(#37, `9e2a33d`), MCP `initialize`-error
    genericization(#36, `ae96c34` — stderr scrub과 동일 정책), web `npm audit`
    (`a0fc935` — 비파괴 수정 없음이 확인됨: 잔여 8건은 전부 breaking major
    (vite 5→8, svelte 4→5)라 별도 업그레이드 사이클로 이관), `config_api.go`
    표면별 분할(#19, `a01d8da` — 순수 이동).
  - **#7 공유 helper 추출은 기각(재분류)**: 독립 검토 2회 결과 restore와
    reRestoreMachine이 모든 이음새(restoreMu 범위, 테스트 시임 cp-wrapper vs 직접
    호출, VsockUDSPath, egress 적용, bind-mount 폴백 vs 에러 반환, 로깅)에서
    갈라져 byte-identical 공유 helper가 성립하지 않음. 기계적 추출 대신
    KEEP-IN-SYNC 주석 유지 + 향후 의도적 설계(hook 주입) 필요 시 재검토.
- 향후 upstream 태그가 `v0.7.0` 이후로 관찰되면 별도 sync/adoption review로 처리한다.
- deferred 항목(default COW 전환, broadcast MCP 노출, auto-snapshot public support,
  flock per-profile sizing)은 각각 KVM burn-in·설계 뒤 결정한다.
