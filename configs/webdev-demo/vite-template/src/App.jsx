import React from 'react';

export default function App() {
  return (
    <main style={{ padding: '4rem', fontFamily: 'system-ui, sans-serif', textAlign: 'center' }}>
      <h1 style={{ color: '#b91c1c' }}>PLACEHOLDER</h1>
      <p>
        This file (<code>src/App.jsx</code>) is the host-side scaffolding placeholder. If
        you see this page in your browser, the agent flock did not publish a replacement
        via the Town Wall sentinel marker. Check the demo logs and the Town Wall transcript
        for the missing <code>&lt;&lt;&lt;FILE: src/App.jsx&gt;&gt;&gt;</code> message.
      </p>
    </main>
  );
}
