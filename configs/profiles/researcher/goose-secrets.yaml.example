# Researcher API keys — keep separate from non-secret config.
# After cp to goose-secrets.yaml, fill in the key for whichever provider
# goose.yaml selects (default: google).

GOOGLE_API_KEY: "your-key-here"
# ANTHROPIC_API_KEY: "sk-ant-your-key-here"
# OPENAI_API_KEY: "sk-your-key-here"
